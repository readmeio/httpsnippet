version: '3'
services:
  httpbin:
    image: kennethreitz/httpbin
  httpsnippet:
    build: .
    command: "npx jest __tests__/integration.test.js"
    environment:
      - HTTPBIN
    depends_on:
      - httpbin
